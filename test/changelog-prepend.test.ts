import { describe, expect, test } from "vitest"

describe("changelog prepend helpers (removed)", () => {
	test("timestamped prepend behavior is no longer part of ai-publish", () => {
		expect(true).toBe(true)
	})
})
