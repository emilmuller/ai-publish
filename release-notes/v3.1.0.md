## v3.1.0

This release improves the reliability of the packaged CLI by adding automated validation during build and publish workflows. It also refines the build output to better match how the CLI is expected to run.

### Highlights

- Updated the build setup to produce a dedicated CLI output in CommonJS format.
